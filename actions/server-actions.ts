"use server"
import { Graphics, Colors } from "@/interfaces/interface"

export async function createNewGraphics(data: Graphics) {
    console.log(data)
}

export async function createNewColor(data: Colors) {
    console.log(data)
}